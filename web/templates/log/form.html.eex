<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <div class="form-group">
    <%= label f, :message, class: "control-label" %>
    <%= text_input f, :message, class: "form-control" %>
    <%= error_tag f, :message %>
  </div>

  <div class="form-group">
    <%= label f, :envivonment, class: "control-label" %>
    <%= text_input f, :envivonment, class: "form-control" %>
    <%= error_tag f, :envivonment %>
  </div>

  <div class="form-group">
    <%= label f, :log_hash, class: "control-label" %>
    <%= text_input f, :log_hash, class: "form-control" %>
    <%= error_tag f, :log_hash %>
  </div>

  <div class="form-group">
    <%= submit "Submit", class: "btn btn-primary" %>
  </div>
<% end %>
